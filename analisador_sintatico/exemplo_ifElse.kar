void main() {
  newVar integer idade;
  newVar boolean permitido;
  idade = 18;

  if((idade > 18)) then {
    permitido = true;
    System.output(permitido);
  };
  if((idade < 18)) then {
    permitido = false;
    System.output(permitido);
  };
}